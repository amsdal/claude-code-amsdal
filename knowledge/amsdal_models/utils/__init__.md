# Module: `amsdal_models.utils`

Package `__init__` — only re-exports `ExternalSchemaConverter` from `amsdal_models.utils.schema_converter`.

## Public API

- `ExternalSchemaConverter` — see `schema_converter.md` for documentation.

`__all__ = ['ExternalSchemaConverter']` — limits `from amsdal_models.utils import *` to just this symbol.

No runtime behavior beyond the re-export.
